# Модель Лоренца
$$
\cases{
\dot x = \sigma (y-x) \\
\dot y = x(r-z) - y \\
\dot z = xy - bz
}
$$
$x$ - скорость вращений конвективных волокон
$y$ - разница температур в слоях
$z$ - отклонение температурной зависимости от линейной

*Биология*: $x, y, z$ - жертва, хищник, паразит
## Исследование
$$
\cases{
-\sigma x + \sigma y = 0 \\
rx - y - xz = 0 \\
xy - bz = 0
}
$$
$$
\cases{
x = y \\
rx - y - xz = 0 \\
xy - bz = 0
}
$$
**Неподвижные точки**
$$
x = y = 0
$$
$$
\cases{
x = y = \pm\sqrt{b (r-1)} \\
z = r - 1  
}
$$
$$
J = \pmatrix{
-\sigma & \sigma & 0 \\
r - z & -1 & -x \\
y & x & -b
}
$$
$$
\lambda_1 = -b
$$
$r > 1: \lambda_2 \lambda_3 < 0, \lambda_2 + \lambda_3 < 0$
$r < 1: \lambda_2 \lambda_3 > 0, \lambda_2 + \lambda_3 < 0$


$x = y = k$
$z = r - 1$
$r > 1$
$\sigma < 1 + b:\ r > -\dfrac{\sigma \left( \sigma + b + 3 \right)}{1 + b - \sigma}$
$\sigma > 1 + b:\ r < - \dfrac{\sigma \left( \sigma + b + 3 \right)}{1 + b - \sigma}$



## Критерий Гурвица?

$$
\alpha_0 \lambda^n + \ldots + \alpha_n = 0
$$
$$
\begin{pmatrix}
\alpha_{1} & \alpha_{3} & 0 & \vdots & 0 & \vdots & 0 \\
\alpha_{0} & \alpha_{2} & 0 & \vdots & 0 & \vdots & \vdots \\
0 & \alpha_{1} & \ddots  & \vdots & 0 & \vdots & \vdots\\
\ldots & \ldots & \ldots & \ddots & \vdots  & \vdots & \vdots\\
0 & 0 & 0 & \ldots & \alpha _{k} & \vdots & 0 \\
\ldots & \ldots & \ldots & \ldots & \ldots & \ddots & \vdots \\
0 & 0 & 0 & \ldots & \ldots & \ldots & \alpha _{n}
\end{pmatrix}
$$




