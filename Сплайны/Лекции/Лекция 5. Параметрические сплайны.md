# Интерполяция кривых
$$
\cases{
x=x(u) \\
y=y(u)
}
$$
$$
u: u_0 < u_1 < \ldots < u_N
$$
$$
\cases{
x_i = x(u_i) \\
y_i = y(u_i)
}
$$
$$
\cases{
S(x, u) \\
S(y, u)
}
$$
$$
R(u) = \sqrt{|S(x, u) - x(u)|^2 + |S(y, u) - y(u)|^2}
$$
## Параметрический сплайн 1 степени
$$
P_i, P_{i+1}
$$
$$
\cases{
S_1(x, s) =  (1 - t) x_i + s x_{i+1}
S_1(y, s) = (1 - t) y_i + s y_{i+1}
}
$$
$$
t = \dfrac{s-s_i}{l_i}
$$
$$
l_i = s_{i+1} - s_i
$$
$$
t \in [0, 1]
$$
**Наклон касательной**
$$
k = \dfrac{S_1'(y, s)}{S_1'(x, s)} = \dfrac{y_{i+1} - y_i}{x_{i+1} - x_i}
$$
**Погрешность**
$$
x(s), y(s) \in CW_{\Delta, \infty}^2 [s_0, s_N]
$$
$$
\| R_1(s) \| \le \dfrac{\sqrt 2}{8} \overline l^2 \|K(s) \|
$$

## Эрмитов сплайн
$$
S_{3, 2}(x, s) = x_i (1-t)^2(1+2t) + x_{i+1} t^2 (3-2t) + x_i' t(1-t)^2 l_i - x_{i+1}' t^2 (1 - t) l_i
$$
**Аппроксимация**
$$
l_i = d_i
$$
$$
x_i' = \dfrac{x_{i+1} - x_i}{d_i}
$$

# Интерполяция поверхностей
**Координатные линии**
$$
L_k \quad k=\overline{0, N_0}
$$
Строим сплайны по линиям
$$
\cases{
S_k(x, s) \\
S_k(y, s) \\
S_k(z, s)
}
$$
Выбираем одинаковое количество точек на каждом сплайне. 
Строим сплайны по точкам для каждого индекса. 
$$
\Delta = \Delta_u \times \Delta_v
$$


